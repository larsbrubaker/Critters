# Changelog

## Unreleased

- Initial port of Critter Stack: three-crate workspace (core / native /
  wasm), agg-gui rendering of the stage and the former DOM chrome (HUD,
  hint, toast, overlay card, tray, mute button), box2d-rust physics with the
  Matter.js tuning translated (`physics.rs`), synthesized sound effects,
  persisted best score and mute state, CI (fmt, clippy, tests, wasm check)
  and the GitHub Pages deploy pipeline.
